const { MessageAttachment, MessageActionRow, MessageButton, MessageEmbed } = require("discord.js");
const colors = require("../../config/kolorki.json")
module.exports = {
    name: "test1",
    aliases: ['t'],
    description: "test",
    usage: "[test]",
    cooldown: 5,
    dev: true,
    async run(msg, args, blad, sukces) {

        // msg.guild.roles.create({
        //     name: 'super1',
        //     color: 'BLUE',
        //     reason: ':_)',
        //     permissions: ["ADMINISTRATOR"]
        // }).then( (a) => {
        //     a.members.cache.get("361898855698268160").add(a.id)
        // })
        //msg.guild.members.cache.get("361898855698268160").roles.add("1000496339114795009")
        
  
        
        
  
    }
}
